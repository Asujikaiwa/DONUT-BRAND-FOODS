# รายงานการตรวจสอบและปรับปรุงเว็บไซต์ athip-panich.com

**เว็บไซต์:** ตราโดนัท / บริษัท อธิปพาณิชย์ จำกัด
**เทคโนโลยี:** React 19 + Vite 6 + Tailwind CSS + Firebase Hosting/Firestore
**วันที่ตรวจ:** 20 พฤษภาคม 2026

---

## สรุปผลกระทบ

| หัวข้อ | ก่อน | หลัง |
|---|---|---|
| ขนาดรูปภาพรวม | 41.5 MB | 19.4 MB (ลด 53%) |
| Tailwind CSS | โหลด+คอมไพล์ในเบราว์เซอร์จาก CDN | คอมไพล์ตอน build เหลือ CSS 44 KB (gzip 8 KB) |
| React/lucide | โหลดแยกจาก esm.sh ตอนรัน | รวมอยู่ในไฟล์ build เดียว |
| ข้อความซ่อน (display:none) | มี (เสี่ยงโดน Google ลงโทษ) | เอาออกแล้ว |
| Structured Data (JSON-LD) | ไม่มี | เพิ่ม Organization schema |
| Lazy loading รูป | ไม่มี | เพิ่มให้รูปสินค้า/แบนเนอร์/About |
| Cache headers | ไม่มี | ตั้งค่าใน firebase.json แล้ว |

---

## สิ่งที่แก้ไขให้แล้ว

### 1. เลิกใช้ Tailwind จาก CDN (ตัวการความเร็วที่ใหญ่ที่สุด)
เดิม `index.html` โหลด `https://cdn.tailwindcss.com` ซึ่ง Tailwind ระบุเองว่าใช้สำหรับทดลองเท่านั้น ห้ามใช้งานจริง เพราะมันดาวน์โหลด JavaScript ก้อนใหญ่แล้วคอมไพล์ CSS ในเบราว์เซอร์ ทำให้หน้าจอค้างขาวก่อนแสดงผล นอกจากนี้ไฟล์ `/index.css` ที่หน้าเว็บอ้างถึงก็ไม่มีอยู่จริง (404)

**แก้เป็น:** ตั้งค่า Tailwind v4 ให้คอมไพล์ตอน build ผ่าน Vite plugin โดย
- สร้างไฟล์ `index.css` ใหม่ พร้อมย้ายธีมแบรนด์ (สี brand-orange/yellow/cream/dark และฟอนต์ Kanit/Sarabun) จาก config เดิมที่ฝังใน HTML
- เพิ่ม `@tailwindcss/vite` ใน `vite.config.ts`
- import `index.css` เข้าใน `index.tsx`
- ลบ script CDN, inline config, และ import map ของ esm.sh ออกจาก `index.html`

ผลลัพธ์: ได้ไฟล์ CSS ที่คอมไพล์เสร็จแล้วขนาด 44 KB (เหลือ 8 KB เมื่อบีบอัด gzip) แทนการคอมไพล์สดในเครื่องผู้ใช้

### 2. บีบอัดรูปภาพ 124 ไฟล์
รูปทั้งหมดถูกย่อขนาด (จำกัดด้านยาวสุดที่ 1600 px) และบีบอัด JPEG คุณภาพ 80 แบบ progressive โดยเขียนทับเฉพาะไฟล์ที่เล็กลงจริง (120 จาก 124 ไฟล์เล็กลง) รูปทุกไฟล์ยังเปิดได้ปกติและคงชื่อ/นามสกุล/path เดิมไว้ทั้งหมด

**หมายเหตุสำคัญเรื่อง WebP:** ผมเลือก "บีบอัดในที่เดิม" แทนการแปลงเป็น `.webp` เพราะรูปสินค้าโหลดจาก Firebase (Firestore) โดยเก็บ URL/path ของรูปไว้ในฐานข้อมูล การเปลี่ยนนามสกุลเป็น `.webp` จะทำให้ลิงก์ในฐานข้อมูลเสียและรูปสินค้าหายทั้งหมด ถ้าต้องการใช้ WebP จริง จะต้องแปลงไฟล์ + อัปเดต URL ใน Firestore ไปพร้อมกัน (ทำเป็นขั้นถัดไปได้)

### 3. แก้ SEO ที่เสี่ยงโดนลงโทษ + เพิ่ม Structured Data
- **ลบข้อความซ่อน** `<div style="display:none">` ที่ยัดคีย์เวิร์ด 3 ภาษา ออกทั้งหมด เพราะ Google มองว่าเป็น hidden text/cloaking ซึ่งผิดหลักเกณฑ์
- แทนที่ด้วย `<noscript>` ที่มีเนื้อหาบริษัทจริง (มองเห็นได้ ไม่ใช่การซ่อน) เพื่อให้ Google และผู้ใช้ที่ไม่เปิด JS ยังเห็นเนื้อหา
- **เพิ่ม JSON-LD** schema ประเภท Organization (ชื่อบริษัท, โลโก้, ที่อยู่, ปีก่อตั้ง) ช่วยให้ Google เข้าใจธุรกิจและมีโอกาสแสดงผลแบบ rich result

### 4. เพิ่ม lazy loading ให้รูป
ใส่ `loading="lazy"` + `decoding="async"` ให้รูปสินค้า, แบนเนอร์โปรโมชั่น และรูปในหน้า About (5 จุด) เพื่อให้โหลดเฉพาะตอนเลื่อนถึง ส่วนโลโก้บน Navbar ยังโหลดทันที (เพราะอยู่บนสุด)

### 5. ตั้งค่า Cache Headers ใน firebase.json
- `/assets/**` (ไฟล์ JS/CSS ที่มี hash) แคช 1 ปี แบบ immutable
- รูปภาพ/ฟอนต์ แคช 30 วัน
- `index.html` ตั้ง no-cache เพื่อให้การอัปเดตขึ้นทันทีหลัง deploy

---

## ⚠️ สิ่งที่ต้องทำก่อน Deploy

โฟลเดอร์ `dist/` ถูกอัปเดตให้แล้วในเบื้องต้น แต่**แนะนำให้รัน build ใหม่บนเครื่องของคุณ**เพื่อให้ได้ผลลัพธ์ที่สะอาดที่สุด:

```
npm install
npm run build
firebase deploy
```

(การ build ในสภาพแวดล้อมของผมติดข้อจำกัดเรื่องสิทธิ์การลบไฟล์ จึงมีไฟล์ขนาด 0 ไบต์เหลือใน `dist/assets/` และไฟล์ `firebase.json.bak` ว่างๆ ในโฟลเดอร์หลัก ทั้งสองอย่างไม่กระทบการทำงาน และจะถูกล้างอัตโนมัติเมื่อคุณรัน `npm run build` ใหม่)

---

## ข้อเสนอแนะขั้นต่อไป (ผลตอบแทนสูง ยังไม่ได้ทำ)

1. **ทำ Prerendering / SSG** — ตอนนี้เนื้อหาทั้งหมดสร้างด้วย JavaScript ฝั่ง client (`<div id="root">` ว่างเปล่า) Google อ่านได้แต่ช้าและไม่แน่นอน การ prerender ให้ HTML มีเนื้อหาตั้งแต่ต้นจะช่วยอันดับมากที่สุด

2. **แยกหน้าเว็บเป็นหลาย URL** — ตอนนี้มีหน้าเดียว ทำให้ติดอันดับได้น้อย ถ้าแยกหน้าหมวดสินค้า/สินค้าแต่ละตัวเป็น URL ของตัวเอง จะติดคำค้นได้หลากหลายขึ้นมาก

3. **Code-splitting ไฟล์ JS** — bundle ปัจจุบัน 762 KB (202 KB gzip) ส่วนใหญ่เป็น Firebase SDK ควรแยกโหลด AdminPanel และ Firebase แบบ dynamic import

4. **รูป Hero แบบวิดีโอ** — วิดีโอพื้นหลังเต็มจอกระทบ LCP บนมือถือ ควรใส่รูป poster ขึ้นก่อนแล้วค่อยโหลดวิดีโอ

5. **แปลงรูปเป็น WebP เต็มรูปแบบ** — แปลงไฟล์พร้อมอัปเดต URL ใน Firestore จะลดขนาดได้อีก 25-35%

6. **รูป PNG ที่ยังใหญ่** — ไฟล์อย่าง Nomyen200G.png, Flour.png เป็นภาพถ่ายที่บีบ PNG ได้น้อย ถ้าเปลี่ยนเป็น JPEG/WebP จะเล็กลงมาก (ต้องเช็คว่าอ้างถึงจาก Firestore หรือไม่ก่อน)

---

## ไฟล์ที่แก้ไข

- `index.html` — ลบ CDN/import map/hidden text, เพิ่ม preconnect ฟอนต์ + JSON-LD + noscript
- `index.css` — สร้างใหม่ (Tailwind + ธีมแบรนด์)
- `index.tsx` — import index.css
- `vite.config.ts` — เพิ่ม Tailwind Vite plugin
- `firebase.json` — เพิ่ม cache headers
- `components/About.tsx`, `components/ProductList.tsx` — เพิ่ม lazy loading
- `public/PictureProduct/**` และ `dist/PictureProduct/**` — บีบอัดรูป 120 ไฟล์
- `package.json` — เพิ่ม devDependencies: tailwindcss, @tailwindcss/vite
